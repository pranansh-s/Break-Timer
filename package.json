{
  "name": "BreakTimer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=public/logo.ico --prune=true --out=release-builds",
    "package-win": "electron-packager . BreakTimer --overwrite --asar=false --platform=win32 --arch=ia32 --icon=public/logo.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=Take a break --version-string.ProductName=\"BreakTimer\"",
    "package-linux": "electron-packager . BreakTimer --overwrite --asar=true --platform=linux --arch=x64 --icon=public/logo.ico --prune=true --out=release-builds"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^11.2.3",
    "electron-packager": "^15.2.0"
  }
}
